# bilstm-ner-nlp
A token-level Named Entity Recognition (NER) system built using BiLSTM + Word2Vec embeddings and NLTK preprocessing. Classifies words into BIO-tagged categories such as person, location, organization, and more.
